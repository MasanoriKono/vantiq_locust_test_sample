# deploy the loucsttest
cd k8s
kubectl apply -f .

# forcibly replace the pod (after changing the scenario, etc..)
kubectl replace --force -f .

# open locust console from terminal
open http://localhost:30089

# undeploy the locusttest
cd k8s
kubectl delete -f .
